name: rdcoletor
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  csv: ^6.0.0
  flutter:
    sdk: flutter
  mobile_scanner:
    git:
      url: https://github.com/BarCollector-Project/mobile_scanner_extended.git
      ref: develop

  path_provider: ^2.1.3 
  path: ^1.9.0
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  file_picker: ^10.2.4
  http: ^1.4.0
  dart_jsonwebtoken: ^3.2.0
  drift: ^2.18.0
  uuid: ^4.4.0
  sqlite3_flutter_libs: ^0.5.22
  sqlite3: ^2.4.0
  simple_barcode_scanner: ^0.3.0
  barcollector_sdk:
    git:
      url: git@github.com:BarCollector-Project/BarCollector-Server.git
      ref: main
      path: SDK


dev_dependencies:
  drift_dev: ^2.18.0
  build_runner: ^2.4.10
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
